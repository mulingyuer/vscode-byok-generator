# GitHub Pages 预览环境变量（执行 vite build --mode github-preview 时加载）
# 项目页部署在子路径下（https://<用户名>.github.io/vscode-byok-generator/），
# base 必须设置为仓库名路径，否则构建出的资源引用会 404 导致白屏
VITE_BASE="/vscode-byok-generator/"
# Cloudflare Worker 代理地址（用于解决浏览器调用sub2api接口跨域问题）
VITE_CORS_PROXY="https://byok-cors-proxy.1321968423.workers.dev/"