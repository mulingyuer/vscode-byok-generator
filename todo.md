# 项目计划

我想要通过web前端可视化的方式来生成一个用于在VSCode的BYOK json配置文件，在vscode中，我们有两种方式来配置BYOK：

1. 通过手动编辑json文件来配置BYOK。
2. 编写一个vscode的BYOK插件，但是插件需要自己处理api的调用等等。

我尝试编写过一个BYOK插件，非常复杂，需处理各种流解析、Tool 调度，需要自己解决协议兼容，但是vscode本身明明就提供了一种非常简单的方式来对接第三方AI模型服务，就是自定义端点（Custom Endpoint）方式，用户只需要配置一个json配置，就可以通过vscode的 Copilot 插件来调用第三方AI模型服务。

这是官方文档：[Custom Endpoint](https://code.visualstudio.com/docs/agent-customization/language-models)

基于这个方式，我觉得生成一个json配置的成本会比编写一个插件要低很多，所以尝试编写一个json配置生成器。

## 主体逻辑

1. 用户输入 `BaseUrl`请求地址。
2. 用户输入 `ApiKey`。
3. 提供两个选项：

   1. 通过openai接口`/v1/models`获取模型列表，然后列表展示，用户进行勾选
   2. 没有接口，从预设官方模型列表中勾选模型

4. 用户勾选完毕后，点击下一步，选择需要访问的协议，这个协议与vscode的协议一致：

   1. `chat-completions`协议
   2. `responses`协议
   3. `messages`协议

5. 协议选择后，点击生成配置文件，此时就会生成一个json配置，用户可以直接复制到vscode的BYOK配置文件中使用。

## 细节

### 用户首个输入界面

1. 输入 `BaseUrl`请求地址。
2. 输入 `ApiKey`。

由于表单较少，我觉得可以固定，不用弹窗，直接在页面展示。

### 获取模型列表界面

用户根据自己的情况，选择获取模型列表的方式。

如果选择的是通过openai接口，那么前端就会发送一个请求，获取模型列表，然后展示给用户，用户进行勾选。

如果选择是预设的模型列表，那么前端就会从预设的模型列表中获取模型列表，然后展示给用户，用户进行勾选。

模型列表的获取方式，可以参考：[OpenAI Models](https://platform.openai.com/docs/models/overview)

### 勾选模型列表

我们需要考虑到模型列表可能会非常多，所以需要提供一些筛选功能，比如：

1. 全选
2. 反选
3. 搜索
4. 好的一个列表展示，多列显示？

用户选择模型列表后，点击下一步，进入下一步。

### 选择协议

考虑到用户可能不明白三个协议的区别，所以需要提供一些提示，比如：

1. `chat-completions`协议：目前用的最多的协议，基本上所有网关都支持。
2. `responses`协议：这个是新的openai标准协议，面向未来，支持多模态，但是部分网关兼容不佳，请根据网关情况选择。
3. `messages`协议：这个是anthropic的协议，claude系列模型支持，也要看网关兼容情况。

用户选择协议后，点击生成配置文件，此时就会生成一个json配置，用户可以直接复制到vscode的BYOK配置文件中使用。

### 生成配置文件

考虑到不同的模型，他们的配置文件会有差异，且我们也需要维护，不可能一个个 `if` 判断，我们我们要维护一个常量对象，里面存储着模型和配置文件的映射关系。

考虑到不同的网关，他们对于模型的id会有差异，我这里举个例子：

1. A网关它的`gpt5.5`的模型id是：`GPT 5.5`
2. B网关它的`gpt5.5`的模型id是：`gpt 5.5`
3. C网关它的`gpt5.5`的模型id是：`Gpt5.5`
4. D网关它的`gpt5.5`的模型id是：`GPT 5.5 ant`
5. E网关它的`gpt5.5`的模型id是：`GPT-5.5`

所以我们要批量生成一个可以匹配的规则，统一小写匹配，然后生成多种规则，如果规则能匹配到网关的模型，那么就可以从map对象中获取预设的配置数据，然后生成json配置文件。

预设的配置数据，我们需要根据AI模型官方提供的文档进行编写，比如：

A模型它的上下文是可以百万Token的，B模型它的上下文是可以十亿Token的，C模型它的上下文是可以二十亿Token的，D模型它的上下文是可以三十亿Token的，E模型它的上下文是可以四十亿Token的。

A模型它的思考只支持：简单、中等、困难

B模型它的思考只支持：简单、中等、困难、高

我们需要根据AI模型官方提供的文档进行编写，同时提供一个思考默认值配置。

### 复制配置文件

考虑到用户可能是首次使用，那么json文件就不能只提供一个对象，需要带上数组包裹：

```json
[
  {
    "name": "智灵 Spirit",
    "vendor": "spirit",
    "apiKey": "${input:chat.lm.secret.2c2b2714}"
  }
]
```

如果已经有配置过，那么就直接复制对象添加到数组中：

```json
{
  "name": "智灵 Spirit",
  "vendor": "spirit",
  "apiKey": "${input:chat.lm.secret.2c2b2714}"
}
```

所以我们复制的时候，要提供两个选项：

1. 全新添加
2. 追加

### 配置文件可编辑

有时候我们生成的配置文件可能不是最合适的，所以需要提供编辑功能，用户可以编辑配置文件，然后自行复制使用。

### 生成一份推荐的模型列表

由于部分网关可能没有提供模型列表接口，所以我们需要提供一份推荐的模型列表，用户可以自行选择。

## 技术选型

1. vue3 + ts + vite + 推荐一个合适的UI组件库
2. 请求使用openai的sdk来获取模型列表，然后展示给用户，用户进行勾选
3. 生成配置文件，先组成对象，然后对象转json字符串。
4. 挑选一个合适的json编辑器，支持代码高亮和json格式报错，以及格式化
